import actions from './actions';
import mutations from './mutations';
import getters from './getters';

// Initial state
const state = {

}

// Export store config used to create the store
export default {
  state,
  getters,
  actions,
  mutations
}
